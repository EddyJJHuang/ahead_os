# Large bundle assets — provided by the SSD bundle, never committed
models/
images/
wheels/
repos/
repos.tar.gz
.venv/
# mock_data: track the small PM OS dataset + KB docs; ignore only big DB binaries
mock_data/**/*.db
mock_data/**/*.sqlite*
# runtime / logs / caches
*.log
__pycache__/
*.pyc
.DS_Store
